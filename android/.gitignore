# Machine-specific SDK path and AMap key
local.properties

# Gradle and Android build output
.gradle/
build/

# Vendored AMap SDK binary (closed-source; fetch from maven.amap.com when it recovers)
app/libs/
app/src/main/jniLibs/

# Release signing keystore (never commit; see local.properties signing.* keys)
app/release.keystore

# IDE metadata
.idea/
*.iml
